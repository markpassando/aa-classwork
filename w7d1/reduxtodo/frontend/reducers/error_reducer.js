
const initialState = [];

const error_reducer = (state = initialState, action) => {
  switch (action.type) {
    case expression:

      break;
    default:
      return state;
  }

};

export default error_reducer;
